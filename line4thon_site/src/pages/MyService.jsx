import React from 'react';
import * as Styled from './MyService.styled';

const MyService = () => {
    return (
        <Styled.Wrapper>
            <Styled.MenuBar>
                <p>내 서비스 페이지</p>
            </Styled.MenuBar>
        </Styled.Wrapper>
    );
};

export default MyService;
