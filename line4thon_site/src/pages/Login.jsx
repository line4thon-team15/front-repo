import React from 'react';
import * as Styled from './Login.styled';

const Login = () => {
    return (
        <Styled.Wrapper>
            <Styled.MenuBar>
                <p>로그인 페이지</p>
            </Styled.MenuBar>
        </Styled.Wrapper>
    );
};

export default Login;
